<{DEB_DIR}>
    <DEBIAN>
        <control type="file">
            <![CDATA[
                Package: {name}
                Version: {version}
                Section: utils
                Priority: optional
                Architecture: {arch}
                Maintainer: Você <voce@email>
                Description: Pacote Rust gerado via Python
            ]]>
        </control>
        <postinst type="file">
            <![CDATA[
                #!/bin/bash
                set -e
                echo "Configurando {name}..."
                chmod +x /usr/local/bin/{name}
            ]]>
        </postinst>
        <prerm type="file">
            <![CDATA[
                #!/bin/bash
                set -e
                echo "Removendo {name}..."
            ]]>
        </prerm>
    </DEBIAN>
    <usr>
        <local>
            <bin name="ddr" type="file" id="binary" src="target/release/ddr"/>
            <share>
                <doc type="file">{name}</doc>
                <man type="file">{name}.1</man>
            </share>
        </local>
    </usr>
</{DEB_DIR}>
